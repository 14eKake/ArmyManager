<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:UI.Resources"
        Title="{x:Static res:Strings.WindowTitle}" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Ressources -->
        <DataGrid ItemsSource="{Binding ResourceStocks}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static res:Strings.Resource}" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="{x:Static res:Strings.Quantity}" Binding="{Binding Amount}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Armées -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,10">
            <DataGrid ItemsSource="{Binding Armies}" SelectedItem="{Binding SelectedArmy}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="120">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static res:Strings.Name}" Binding="{Binding Name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid ItemsSource="{Binding SelectedArmy.Stacks}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="120" Margin="0,5,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static res:Strings.Unit}" Binding="{Binding Type.Name}" Width="*"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Quantity}" Binding="{Binding Quantity}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBox Width="150" Text="{Binding NewArmyName, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="{x:Static res:Strings.Create}" Command="{Binding CreateArmyCommand}" Margin="5,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Entraînement -->
        <StackPanel Grid.Row="2">
            <DataGrid ItemsSource="{Binding UnitTypes}" SelectedItem="{Binding SelectedUnitType}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="180">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static res:Strings.Name}" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="{x:Static res:Strings.Attack}" Binding="{Binding Attack}" Width="Auto"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Defense}" Binding="{Binding Defense}" Width="Auto"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Speed}" Binding="{Binding Speed}" Width="Auto"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Cost}" Binding="{Binding CostDisplay}" Width="Auto"/>
                    <DataGridTextColumn Header="{x:Static res:Strings.Manpower}" Binding="{Binding ManpowerCost}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="{x:Static res:Strings.QuantityLabel}" VerticalAlignment="Center"/>
                <TextBox Width="40" Text="{Binding TrainingQuantity, UpdateSourceTrigger=PropertyChanged}" Margin="5,0"/>
                <Button Content="{x:Static res:Strings.Train}" Command="{Binding TrainCommand}" Margin="5,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
